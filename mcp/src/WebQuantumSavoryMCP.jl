module WebQuantumSavoryMCP

# The executable implementation lives in `mcp/main.jl`. This module exists so
# Julia's package manager can instantiate and precompile the isolated sidecar
# project like an ordinary Julia application environment.

end
